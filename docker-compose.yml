version: '3.8'
services:
  agent-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
    # This allows the container to talk to your host machine's Ollama!
    extra_hosts:
      - "host.docker.internal:host-gateway"